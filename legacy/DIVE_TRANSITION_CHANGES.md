# 潜る演出のリニューアル (dive-flow-28)

## 28 の変更（参照画像に寄せる: 見上げた水面・浮かぶログピル・水面チップ）

参照画像（水面/潜る の2画面モック）との視覚差分を詰めた回。

### 潜る画面（Canvas: `drawUnderwaterSurface` 新設）
- **下から見上げた水面**: 潜り切る頃（`diveProgress` 0.55→1でフェードイン）、水面ラインの**上側が「水の裏側」になる**。暗い波の背・ラインへ向かって明るくなる帯（下端はラインと同じ揺れに追従）・月光が水面越しに溜まる光だまり。
- **ゴッドレイ**: ラインから下へ、ゆっくり揺れる3本の細い光の筋。ラインに追従するので、ログをスクロールして水面が上へ抜けると一緒に消えていく（`diveScrollDepth` でも減光）。
- DOMの光柱 `.dive-light` は控えめに（0.76→0.5）。

### 潜る画面（DOM）
- ログを「左右カード＋横の点＋中央の縦線」→ **中央付近に浮かぶ小さなピル**へ。中央揃え・fit-content・左右に少しだけオフセット・角丸18px・薄い縁と淡い発光。各ピルの下に**点線の揺らめき**（沈殿するプランクトン）。中央の縦線と横の点は削除。
- 深いログほど**より暗く小さく**（opacity 0.96→0.3 / scale →0.86）。
- ヘッダーの「Dive log / 潜る」見出しを非表示（参照画像に見出しは無い）。「水面へ」はより薄いゴースト調に。
- 最下部に**⌄⌄（さらに深くある）インジケータ**を追加。ゆっくり上下し、スクロールすると消える（`--dive-scroll` 連動）。記録が4件未満なら非表示。

### 水面画面
- 最下部に**直近3件の記録チップ**（例: 勉強 1h）＋その上に細いハンドル線。参照画像の下端UIの再現。表示のみ（タップ不可）、潜る時は他のUIと一緒にフェード。
- チップと重なるため「記録を足す」を上へ（desktop 24→66px / mobile 18→88px）。

### `legacy/index.html`
- `#surfaceChips` / `#diveMore` を追加。キャッシュバスター `dive-flow-27` → `dive-flow-28`。

## 微調整のツマミ（28）
- 見上げた水面の出る間合い → `drawUnderwaterSurface` の `(diveProgress - 0.55) / 0.45`
- 光だまりの強さ → `pool` の `0.2 * strength`
- ゴッドレイの本数・太さ・明るさ → 同関数のループ（`0.085/0.05 * strength`）
- ログピルのオフセット → `.dive-log-item.is-left/right` の `translateX`
- チップの件数 → `renderSurfaceChips()` の `.slice(0, 3)`

以下は dive-flow-27 の内容。

---

# 潜る演出のリニューアル (dive-flow-27)

## 27 の変更（深くスクロールすると水面が上へ抜けて消える）

- Dive Log を下にスクロール（＝さらに深く潜る）すると、**水面ラインが上へ抜けて画面外に消える**ように。潜っているのに水面が見え続ける違和感を解消。
- 仕組み: スクロール量 `diveScrollDepth`（`updateDiveScrollDepth` で取得、フレームで補間）を Canvas のカメラ送り量に加算（`scrollLift = diveScrollDepth × height × 0.18`）。`sceneLift = 潜降分 + scrollLift` で水面ライン（とdeepening開始位置 `lineY`）を上へ。
- スクロールを上に戻すと水面が降りてきて再び見える（対称）。「水面へ」を押すと `diveScrollTarget=0` で持ち上がりも自然に戻る。

以下は dive-flow-26 の内容。

---

# 潜る演出のリニューアル (dive-flow-26)

## 26 の変更（泡の量・消える位置・フェード長を調整）

- **数を減らす**: 1塊あたり `1〜3個`（以前は3〜9）、発生間隔も `1.8〜6.2秒`（以前は0.7〜4.3）に延長。全体に疎で控えめに。
- **消える位置を上げる**: 水面ライン手前 → **`height × 0.42`（画面中央より少し上）**で消えるように。
- **フェードを長く**: 消えるまでのグラデーション距離を `64px` → **`height × 0.24`** に拡大。スッとではなく、ゆっくり薄れて消える。

以下は dive-flow-25 の内容。

---

# 潜る演出のリニューアル (dive-flow-25)

## 25 の変更（泡の色・動き・退場を調整）

- **色を背景に馴染ませる**: 白っぽい `rgba(176,226,255)` → 深い水に近い落ち着いた青 `rgba(116,164,202)`（ハイライトも `rgba(150,192,222)` に）。不透明度も `0.2+0.24` → `0.16+0.16` に低減。
- **ほぼ真上に**: 左右ゆれ `wobbleAmp 4〜16 / rate 1〜2.8` → `1.5〜4 / 0.5〜1.4` に減らし、ごく緩い自然なゆらぎだけ残す（直線的すぎない）。
- **「水面へ」で静かに消える**: 泡全体のフェード係数 `bubbleFade` を追加。`diveTarget===1` で1へ、ボタンで `0` になった瞬間に約0.4秒で0へイージング。残留せず静かに消える。生成も潜水中（`diveTarget===1`）のみ。

以下は dive-flow-24 の内容。

---

# 潜る演出のリニューアル (dive-flow-24)

## 24 の変更（泡を潜水中のみ・画面外から・大きくランダムに）

- **水面画面では泡オフ**: 泡を `diveProgress > 0.01` のときだけ（＝潜水中＝Dive Logのみ）に。水面画面（`diveProgress=0`）では出さない＋配列クリア。
- **画面外（底辺）から発生**: 生成位置を `height + 12..60`（画面の下＝オフスクリーン）に変更。スクリーン座標で描画し、画面の底から湧き上がって水面ライン（`lineY`）手前で消える。
- **大きく・ランダムなサイズ**: 半径 `2 + random*5`（約2〜7px、以前は0.8〜3.4）。
- 描画はCanvasのため、Dive Logの背景（`.dive-view`）を**薄いベール**に変更し、泡が画面全体で見えるように（暗さはCanvas側の deepening が担当）。

以下は dive-flow-23 の内容。

---

# 潜る演出のリニューアル (dive-flow-23)

## 23 の変更（夜光虫を潜降中のみに＋常時の泡アニメ追加）

- **夜光虫（潜降の上昇ストリーク, `diveBubbles`）**: `diveProgress` 連動 → **フレア（`sin(diveProgress*π)`）連動**に変更。潜降アニメ中だけ表示し、**潜り切ると自動的にオフ**。さらに**水面ライン（`lineY`）より上には行かず**、水面手前80pxでフェードアウトして下へリサイクル。
- **泡アニメを新規追加（常時）**: 水中を下から上へ昇る泡（`bubbles`）。
  - 発生は一定ではなく、**ランダムな間隔（0.7〜4.3秒）でまとまった塊（3〜9個）**を海底のランダムな位置から放出（`spawnBubbleCluster`）→ 密度が時間で揺らぐ。
  - ゆっくり上昇＋横ゆらぎ。**水面手前でフェードアウトして消える**（水面より上へは出ない。`clipWater` でも水中にクリップ）。

以下は dive-flow-22 の内容。

---

# 潜る演出のリニューアル (dive-flow-22)

## 22 の変更（Canvasの水平線をDive Logにそのまま残す）

- Dive Log上部のCSS製ニセ水面ライン（`.dive-surface` 一式）と `index.html` の `<div class="dive-surface">` を**削除**。
- `.dive-view` の背景を**上部だけ透明**（0〜78pxは透明 → 下へ向かって暗転）にし、**Canvasが描いている本物の水平線（潜降で上部にキープ済み）をそのまま透過表示**。＝似たものを作るのではなく、同じ線を残す。
- 線の発光・波の平坦化・粒子の集約を **`diveProgress` 単調 → 山なりのフレア（`sin(diveProgress*π)`）** に変更。潜降アニメ中だけ発光・集約が起き、Dive Logで落ち着くと**水面画面と同じ明るさ・波**に戻る（＝そのまま）。線の位置（上端キープ）と下方の暗転は従来通り `diveProgress` 連動で維持。
- 未使用になった `@keyframes dive-surface` / `surface-horizon-breathe` を削除。

以下は dive-flow-21 の内容。

---

# 潜る演出のリニューアル (dive-flow-21)

## 21 の変更（Dive Log上部の水面ラインを水平線と同デザインに）

- Dive Log 上部の水面ライン（CSS: `.dive-surface::before`）を、**Canvasの水平線（`traceSurfaceLine`）と同じ配色・発光**に統一。シアンの芯→中央右でバイオレットへ（グラデ44%/55%）、シアンのブルーム。
- 潜行アニメ中に発光する水平線はCanvas側のまま（`diveBoost`）で**変更なし＝キープ**。

以下は dive-flow-20 の内容。

---

# 潜る演出のリニューアル (dive-flow-20)

## 20 の変更（沈む感じの強化＋水平線の右端切れ修正）

- **沈降感アップ**: 潜降中に **夜光虫が上へ流れる前景レイヤー（marine snow）** を追加（`diveBubbles` / `updateAndDrawDiveBubbles`）。前景が別速度で上へ流れることで視差が生まれ、「自分が下へ沈んでいる」感覚が強まる（深くなるほど速く・濃く）。
- **バグ修正**: 水面の水平線の**右端が切れる**問題を修正。`traceSurfaceLine` / `traceWaterPath` が16/18px刻みのループで、画面幅が刻みの倍数でないと右端が描かれていなかった。末尾で `bounds.right` まで明示的に描くように。

以下は dive-flow-19 の内容。

---

# 潜る演出のリニューアル (dive-flow-19)

「潜る」演出を **Canvas（実際の海が描かれている場所）の中で起こす** 方式。
対象は `legacy/` の静的プロトタイプ（公開先: `/Luminous-Tide/legacy/`）。

## これまでの経緯

- 16/17: 水中ビューのDOM要素を動かして潜航を表現 → 「ポップアップがバラバラ／カクカク」。
- 18: 演出をCanvasへ移しカクつきは解消。ただし線へズームして暗転する作りで「沈んでる感じがしない／水平線が消える」。
- **19（今回）**: 線を消さず、**明るいまま画面上方へ昇らせて水の断面（天井）として残す**。ズームをやめて**シーン全体をゆっくり上へ送る＝視点が下降**する動きに。速度も遅く。

## 演出（Canvas主導）

1. **集約**: 「潜る」で `diveProgress`(0→1) が一定速度で立ち上がる。夜光虫（粒子）が水平線へ吸い寄せられ、波の振幅が0に近づき、発光ラインが一本に集約・増光。
2. **下降**: シーン全体を上方向へ平行移動（`translate`）し、水平線を画面上部（`DIVE_LINE_TOP=44px`）へ昇らせる。**線は消さずキープ＝水の断面**。線より下は徐々に暗くなり「深く沈む」感を出す。
3. **Dive Log**: 線を越えたあと（`diveProgress>0.82`）に、水中画面を1枚で静かにフェードイン。Dive Log側の水面ライン（上端42px）と着地位置を合わせ、線が連続する。

## 今回の変更点（18→19）

### `legacy/app.js`
- 補間を**時間ベースの一定速度**へ: `DIVE_IN_RATE = 1/2.4`（潜降 約2.4秒）/ `DIVE_OUT_RATE = 1/1.5`（浮上 約1.5秒）。以前の指数補間（速すぎた）を廃止。
- `frame()`: ズーム＋全画面暗転をやめ、**`translate` でシーンを上へ送る**だけに。水平線の着地位置 `DIVE_LINE_TOP=44`。
- 線より下だけを暗くする**deepening グラデーション**を追加（線は明るいまま残す＝水の断面）。
- 潜降中はシーンが上に動くため、背景塗りが下端を覆わなくなる → **画面クリアを追加**してゴースト（残像）防止。
- `updateDiveStage()` を新設し、Dive Log の表示・状態クリアを **`diveProgress` のしきい値で同期**（`setTimeout` を全廃）。
- `openDiveView()`/`closeDiveView()` は `diveTarget` を 1/0 にするだけのシンプル構成に。

### `legacy/index.html`
- キャッシュバスターを `dive-flow-18` → `dive-flow-19`。

## 微調整のツマミ

- 潜降の速さ → `DIVE_IN_RATE`（`1/2.4` の 2.4 を増やすと遅く）
- 線が昇りきる位置（水の断面の高さ）→ `DIVE_LINE_TOP`
- 線への吸い寄せの強さ → `updateParticles()` の `pull * 0.16`
- 線の明るさ → `drawWaterBase()` の `diveBoost = 1 + diveProgress * 3.4`
- 深みの暗さ・出方 → `frame()` の deepening グラデーション（`0.55 * deep` / `0.98 * deep`）
- Dive Log が出る間合い → `updateDiveStage()` の `diveProgress > 0.82`

## 確認方法

mainへpush → GitHub Pages がビルド。携帯で `/Luminous-Tide/legacy/` を開き「潜る」を試す（検証は実機で）。
